## 0.0.1

* Initial release of the material_window_size package.
* Provides `WindowWidthClass` and `WindowHeightClass` enums based on Material 3 guidelines.
* Includes `WindowSizeClass` to combine width and height classes.
* Utility methods for querying size classes from `BuildContext`.
